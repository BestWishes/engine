server:
  port: 10401
spring:
  application:
    name: mobile
  cloud:
    consul:
      port: 8500
      host: 127.0.0.1
    config:
      label: master
      discovery:
        enabled: true
        service-id: config-server
      profile: dev